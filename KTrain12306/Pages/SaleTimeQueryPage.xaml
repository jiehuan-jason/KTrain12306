<Page
    x:Class="KTrain12306.Pages.SaleTimeQueryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KTrain12306.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:KTrain12306"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <SplitView x:Name="splitView" PaneBackground="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
           IsPaneOpen="False" OpenPaneLength="256" CompactPaneLength="48" DisplayMode="CompactOverlay">
        <SplitView.Pane>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ListView x:Name="NavLinksList" Margin="0,12,0,0" SelectionMode="None" Grid.Row="1" VerticalAlignment="Stretch"
                    ItemClick="NavLinksList_ItemClick"  IsItemClickEnabled="True">
                    <StackPanel x:Name="menu" Orientation="Horizontal" Grid.Row="0" Margin="0,25,0,24" Height="15" >
                        <SymbolIcon Symbol="More"  HorizontalAlignment="Stretch" Height="20" Margin="0,0,0,0" VerticalAlignment="Center" />
                        <TextBlock Text="菜单" Margin="24,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel x:Name="home" Orientation="Horizontal" Grid.Row="0" Margin="0,10,0,10" Height="20">
                        <SymbolIcon Symbol="Home"  HorizontalAlignment="Stretch" Height="20" Margin="0,0,0,0" VerticalAlignment="Center" />
                        <TextBlock Text="主页" Margin="24,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel x:Name="time" Orientation="Horizontal" Grid.Row="0" Margin="0,10,0,0"  Height="20">
                        <SymbolIcon Symbol="Clock" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,0" Width="21" />
                        <TextBlock Text="开售时间" Margin="23,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </ListView>
            </Grid>
        </SplitView.Pane>


        <Grid x:Name="body">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel x:Name="head" Orientation="Horizontal" Grid.Row="0">
                <TextBlock x:Name="title" Text="开售时间" FontSize="60" Margin="10,0,0,0" VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel Grid.Row="1">
                <Button x:Name="station" Background="#00000000" HorizontalAlignment="Center" Content="香港西九龙" Click="Station_Click" FontSize="20"/>
                <CalendarDatePicker x:Name="calendar" HorizontalAlignment="Center" Grid.Row="1" PlaceholderText="选择日期"/>
                <Button x:Name="search" Content="查询" Click="Search_Click" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
            <ProgressRing x:Name="LoadingRing" IsActive="False" Width="100" Height="100"
                  HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10,10,10,10" Grid.Row="2"/>
            <ListView x:Name="TimeList" HorizontalAlignment="Center" VerticalAlignment="Stretch" SelectionMode="None" IsItemClickEnabled="False" Margin="10,10,10,10" Grid.Row="2">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="model:SaleTime">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontSize="15" Text="{Binding name}" Margin="0,0,10,0" />
                            <TextBlock FontSize="15" Text="{Binding value}" Margin="10,0,0,0" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

    </SplitView>
</Page>
