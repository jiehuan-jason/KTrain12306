<Page
    x:Class="KTrain12306.TrainInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KTrain12306"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel HorizontalAlignment="Center">
        <StackPanel x:Name="head" Orientation="Horizontal">
            <TextBlock x:Name="title" Text="列车信息" FontSize="70" Margin="10,10,0,0" Height="114" />
        </StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="train_code" FontSize="40" Margin="10,10,10,10"/>
            <TextBlock Grid.Row="1" Grid.Column="0" x:Name="from_station_name" FontSize="30" Margin="10,10,10,10" HorizontalAlignment="Left"/>
            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="time" FontSize="20" Margin="10,10,10,10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="1" Grid.Column="2" x:Name="to_station_name" FontSize="30" Margin="10,10,10,10" HorizontalAlignment="Right"/>
        </Grid>
        <StackPanel>
            
        </StackPanel>
    </StackPanel>
</Page>
