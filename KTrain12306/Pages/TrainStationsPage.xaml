<Page
    x:Class="KTrain12306.Pages.TrainStationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KTrain12306.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local1="using:KTrain12306.Model"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" x:Name="head" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,0,10,0">
            <TextBlock x:Name="title" Text="列车信息" FontSize="70" Margin="10,10,0,0" />
        </StackPanel>
        <ProgressRing x:Name="LoadingRing" Grid.Row="1" IsActive="False" Width="100" Height="100"
                  HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,10,10,10"/>
        <Grid Grid.Row="1" HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Row="0" Grid.Column="1" x:Name="train_code" FontSize="40" Margin="10,10,10,10" Background="Transparent"/>
                <TextBlock Grid.Row="1" Grid.Column="0" x:Name="from_station_name" FontSize="30" Margin="10,10,10,10" HorizontalAlignment="Left"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="time" FontSize="20" Margin="10,10,10,10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" Grid.Column="2" x:Name="to_station_name" FontSize="30" Margin="10,10,10,10" HorizontalAlignment="Right"/>
                <TextBlock Grid.Row="2" Grid.Column="0" x:Name="start_time" FontSize="20" Margin="10,10,10,10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <StackPanel Grid.Row="2" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock x:Name="arrive_time" FontSize="20" Margin="10,10,10,10"/>
                    <TextBlock x:Name="add_day_display" FontSize="12" Margin="-5,0,0,-5"/>
                </StackPanel>
            </Grid>
        <ListView x:Name="StationsList" Grid.Row="2"  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="10,10,10,10">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local1:TrainStation">
                    <Grid HorizontalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding station_name}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,10,0"/>
                        <TextBlock Grid.Column="1" Text="{Binding arrive_time}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0,10,0"/>
                        <TextBlock Grid.Column="2" Text="{Binding start_time}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0,10,0"/>
                        <TextBlock Grid.Column="3" Text="{Binding arrive_day_str}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10,0,10,0"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
